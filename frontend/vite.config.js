import { defineConfig } from "vite";

// If your GitHub Pages site is served from the root domain (ahamstree.com),
// base should be '/'. If using username.github.io/repo, set base: '/repo/'.
export default defineConfig({
  base: "/"
});

